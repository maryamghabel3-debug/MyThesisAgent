"""
عامل نویسنده (Writer) — نگارش متن پایان‌نامه بر اساس منابع مصوب.

این عامل پیش‌نویس هر بخش را صرفاً بر اساس منابع تأییدشده توسط
عامل پژوهشگر و ساختار مصوب در OUTLINE.md می‌نویسد و در صورت نیاز
متن را تا رسیدن به تعداد کلمات هدف بسط می‌دهد.
"""


class WriterAgent:
    """عامل نویسنده: تولید پیش‌نویس بخش‌های پایان‌نامه."""

    def draft_section(self, sources, outline_section):
        """
        پیش‌نویس یک بخش از پایان‌نامه را بر اساس منابع و سرفصل مصوب می‌نویسد.

        ورودی‌ها:
            sources: منابع تأییدشده توسط عامل پژوهشگر.
            outline_section: سرفصل و جزئیات بخش مورد نظر از ساختار مصوب.

        خروجی:
            متن پیش‌نویس بخش.

        این متد در مراحل بعدی تکمیل خواهد شد.
        """
        raise NotImplementedError("این متد در مراحل بعدی پیاده‌سازی می‌شود.")

    def expand_text(self, text, target_words):
        """
        متن را بدون تغییر محتوا، تا رسیدن به تعداد کلمات هدف بسط می‌دهد.

        ورودی‌ها:
            text: متن فعلی.
            target_words: تعداد کلمات هدف.

        خروجی:
            متن بسط‌یافته.

        این متد در مراحل بعدی تکمیل خواهد شد.
        """
        raise NotImplementedError("این متد در مراحل بعدی پیاده‌سازی می‌شود.")
