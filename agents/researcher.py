"""
عامل پژوهشگر (Researcher) — جست‌وجو و اعتبارسنجی منابع علمی.

این عامل منابع علمی مرتبط با موضوع پایان‌نامه را پیدا می‌کند،
میزان ارتباط آن‌ها را می‌سنجد، شناسهٔ DOI را اعتبارسنجی می‌کند
و متادیتای هر منبع را برای ذخیره در data/metadata.json استخراج می‌نماید.
"""


class ResearcherAgent:
    """عامل پژوهشگر: یافتن، ارزیابی و اعتبارسنجی منابع علمی."""

    def search_sources(self, keywords, core_concepts):
        """
        منابع علمی مرتبط با کلیدواژه‌ها و مفاهیم کلیدی را جست‌وجو می‌کند.

        ورودی‌ها:
            keywords: کلیدواژه‌های مورد نظر برای جست‌وجو.
            core_concepts: مفاهیم اصلی پایان‌نامه برای سنجش ارتباط.

        خروجی:
            فهرست منابع کاندید.

        این متد در مراحل بعدی تکمیل خواهد شد.
        """
        raise NotImplementedError("این متد در مراحل بعدی پیاده‌سازی می‌شود.")

    def relevance_check(self, abstract, core_concepts):
        """
        میزان ارتباط چکیدهٔ یک مقاله با مفاهیم کلیدی پایان‌نامه را می‌سنجد.

        ورودی‌ها:
            abstract: چکیدهٔ مقاله.
            core_concepts: مفاهیم اصلی پایان‌نامه.

        خروجی:
            نتیجهٔ ارزیابی ارتباط (تأیید یا رد).

        این متد در مراحل بعدی تکمیل خواهد شد.
        """
        raise NotImplementedError("این متد در مراحل بعدی پیاده‌سازی می‌شود.")

    def verify_doi(self, doi):
        """
        اعتبار شناسهٔ DOI یک منبع را راستی‌آزمایی می‌کند.

        ورودی‌ها:
            doi: شناسهٔ دیجیتال منبع.

        خروجی:
            وضعیت معتبر بودن DOI.

        این متد در مراحل بعدی تکمیل خواهد شد.
        """
        raise NotImplementedError("این متد در مراحل بعدی پیاده‌سازی می‌شود.")

    def extract_metadata(self, paper):
        """
        متادیتای یک منبع (عنوان، نویسندگان، سال، مجله و…) را استخراج می‌کند.

        ورودی‌ها:
            paper: شیء یا دادهٔ خام منبع.

        خروجی:
            دیکشنری متادیتای استانداردشده.

        این متد در مراحل بعدی تکمیل خواهد شد.
        """
        raise NotImplementedError("این متد در مراحل بعدی پیاده‌سازی می‌شود.")
