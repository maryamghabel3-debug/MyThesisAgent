# وابستگی‌های پروژهٔ MyThesisAgent
# نصب: pip install -r requirements.txt

# کلاینت مدل زبانی سازگار با OpenAI (پیکربندی از متغیرهای محیطی)
openai

# خواندن و نوشتن فایل‌های Word — استخراج متن پروپوزال و خروجی فصل‌ها
python-docx

# درخواست‌های HTTP — جست‌وجوی منابع علمی و اعتبارسنجی DOI در مراحل بعدی
requests
